import { LucideProps, UserCircle, Menu, Settings, LogOut, Home } from "lucide-react"

export type Icon = typeof UserCircle

export const Icons = {
  logo: Home,
  user: UserCircle,
  menu: Menu,
  settings: Settings,
  logout: LogOut,
}
